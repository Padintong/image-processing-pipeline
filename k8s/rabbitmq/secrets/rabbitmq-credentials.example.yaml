# Copy this file to rabbitmq-credentials.yaml (gitignored — see **/secrets/*.yaml in .gitignore)
# and replace both values before applying. Never commit the real file.
apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-credentials
  namespace: image-pipeline
type: Opaque
stringData:
  username: CHANGE_ME
  password: CHANGE_ME
